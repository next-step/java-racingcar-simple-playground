## 1단계 - 움직이는 자동차

### 기능 요구사항

- 자동차는 이름을 가지고 있다.
- 자동차는 움직일 수 있다.
- 0에서 9 사이에서 random 값을 구한 후 random 값이 4 이상일 경우 전진하고, 3 이하의 값이면 멈춘다.

### 테스트 요구사항

- 자동차가 움직이는 기능이 의도대로 동작하는지 테스트한다.

### 구현에 대한 고민

- 자동차를 생성, 관리하고 움직이는 주체에 대해 고민하며, 자동차 게임을 진행하는 GameManager 객체를 만들었습니다.

- 게임의 전체 흐름을 GameManager 객체의 play 메소드에서 담당해야 할지, 
  main 메소드에서 GameManager 메소드 호출을 통해 진행해야 할지 고민했습니다.
  - 실행 주체는 사람이므로 main 메소드 내에서 게임 진행에 필요한 메소드를 호출하는 
    방향이 더 자연스럽다고 생각하여 GameManager의 역할을 분리하여 구현하였습니다.
  - 또한 메서드가 한 가지 일만 할 수 있게 해야하므로 전체 게임의 흐름은 따로 분리하는 게 
    낫다고 생각했습니다.

- GameManager 테스트 범위
  - 랜덤 값 생성에 대한 테스트를 할 때, 랜덤 값이 잘 생성되느냐는 GameManager의 검증 영역에서 벗어났다고 생각했습니다.
  - 랜덤 값을 미리 정해두고, 전진 메소드가 정해진 랜덤 값을 기준으로 잘 동작하느냐를 중심으로 테스트 코드를 작성했습니다.


### GameManager 역할 분리

- 사용자에게 Car 이름 입력받기 
- Car 객체 생성
- 랜덤 값 구하기
- 전진 여부 판별(헬퍼 메소드) 후 자동차를 전진시키기
- 자동차의 위치를 나타내기



## 2단계 - 우승 자동차 구하기

### 추가된 기능 요구사항

- n대의 자동차가 참여할 수 있다.
- 주어진 횟수 동안 n대의 자동차는 전진 또는 멈출 수 있다.
- 자동차 경주 게임을 완료한 후 누가 우승했는지를 구할 수 있다. 우승자는 한 명 이상일 수 있다.

### 테스트 요구사항

- 우승자를 구하는 기능이 의도대로 동작하는지 테스트한다.

### 구현에 대한 고민

- 기존에 구현한 GameManager.getWinCars() 에서 우승자를 구하기 위해 GameManager 내부의 멤버 변수인 cars 객체를 
  this를 통해 GameManager 클래스 내부에서 가져왔습니다.
- 하지만 테스트코드를 짜면서 해당 메서드만 테스트하기가 용이하지 않아, getWinCars 메서드에 파라미터로 List<Car>를 받아
  테스트에 용이한 메소드로 시그니처를 변경하였습니다.
- 단위 테스트의 편리성을 위하여 GameManager 내부에서 우승자를 구하는 로직(getWinCars)을 외부에 공개해도 되는지 고민하였습니다.
- 테스트 내부에서 해당 메소드의 기능만 따로 테스트할 수 있는 다른 방법은 없는지에 대해서도 궁금합니다.


## 3단계 - 게임 실행

### 추가된 요구사항

- 각 자동차에 이름을 부여할 수 있다. 전진하는 자동차를 출력할 때 자동차 이름을 같이 출력한다.
- 자동차 이름은 쉼표(,)를 기준으로 구분하며 이름은 5자 이하만 가능하다.
- 사용자는 몇 번의 이동을 할 것인지를 입력할 수 있어야 한다.
- 메인 메서드를 추가하여 실행 가능한 애플리케이션으로 만든다.

### 구현에 대한 고민

- GameManager 의 역할이 너무 커지는 것 같아 입출력 역할을 담당하는 클래스를 분리하였습니다.
- 매 경기를 출력하기 위해, 한 경기마다의 결과(상태)를 어디에 저장해야 하는지 고민했습니다.
  - ConsoleView 가 그 상태를 저장하게 하면, 입출력만의 역할을 가지지 않고 저장소 역할을 하게 될 것 같아 제외하였습니다.
  - 기존 GameManage 의 race 메소드를 Main 메소드에서 매 라운드 후 Cars 객체를 넘겨 라운드마다 결과를 출력하게 하였습니다.