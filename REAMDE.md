## 1단계 - 움직이는 자동차

### 기능 요구사항

- 자동차는 이름을 가지고 있다.
- 자동차는 움직일 수 있다.
- 0에서 9 사이에서 random 값을 구한 후 random 값이 4 이상일 경우 전진하고, 3 이하의 값이면 멈춘다.

### 테스트 요구사항

- 자동차가 움직이는 기능이 의도대로 동작하는지 테스트한다.

### 구현에 대한 고민

- 자동차를 생성, 관리하고 움직이는 주체에 대해 고민하며, 자동차 게임을 진행하는 GameManager 객체를 만들었습니다.

- 게임의 전체 흐름을 GameManager 객체의 play 메소드에서 담당해야 할지, 
  main 메소드에서 GameManager 메소드 호출을 통해 진행해야 할지 고민했습니다.
  - 실행 주체는 사람이므로 main 메소드 내에서 게임 진행에 필요한 메소드를 호출하는 
    방향이 더 자연스럽다고 생각하여 GameManager의 역할을 분리하여 구현하였습니다.
  - 또한 메서드가 한 가지 일만 할 수 있게 해야하므로 전체 게임의 흐름은 따로 분리하는 게 
    낫다고 생각했습니다.

- GameManager 테스트 범위
  - 랜덤 값 생성에 대한 테스트를 할 때, 랜덤 값이 잘 생성되느냐는 GameManager의 검증 영역에서 벗어났다고 생각했습니다.
  - 랜덤 값을 미리 정해두고, 전진 메소드가 정해진 랜덤 값을 기준으로 잘 동작하느냐를 중심으로 테스트 코드를 작성했습니다.


### GameManager 역할 분리

- 사용자에게 Car 이름 입력받기
- Car 객체 생성
- 랜덤 값 구하기
- 전진 여부 판별(헬퍼 메소드) 후 자동차를 전진시키기
- 자동차의 위치를 나타내기


